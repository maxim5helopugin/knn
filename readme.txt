Random Projection Tree K-NN

Creates a Random Projection Tree from the dataset and classifies the test set with guided priority search.
To run just run "run_tests.py" script.

*MNIST and SVHN datasets not included, too large for github